# Database
# Set DATABASE_URL to a PostgreSQL connection string to use PostgreSQL.
# If DATABASE_URL is not set (or is not a postgresql:// URL), the server
# automatically falls back to a local SQLite database (file:./opet.db).
DB_USER=opet
DB_PASSWORD=change-me-db-password
DB_NAME=opet

# Uncomment to use PostgreSQL:
# DATABASE_URL=postgresql://opet:change-me-db-password@localhost:5432/opet
# Leave unset (or omit) to use the SQLite fallback:
# DATABASE_URL=file:./opet.db
JWT_SECRET=change-me-jwt-secret-min-32-chars
PORT=3001
NODE_ENV=development
APP_URL=http://localhost:3000

# Optional: run RSS/news import automatically every N minutes.
# Leave 0 to use manual imports from the admin Pressespiegel page only.
NEWS_IMPORT_INTERVAL_MINUTES=0

# SMTP (email)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=your-smtp-password
FROM_EMAIL=noreply@example.com

# Admin seed (used by prisma/seed.ts)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme-admin-password

# Frontend
VITE_API_URL=http://localhost:3001
