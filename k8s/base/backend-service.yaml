apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: opet
  labels:
    app.kubernetes.io/name: opet-backend
    app.kubernetes.io/component: backend
spec:
  selector:
    app.kubernetes.io/name: opet-backend
  ports:
    - name: http
      port: 3001
      targetPort: http
