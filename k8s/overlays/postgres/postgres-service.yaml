apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: opet
  labels:
    app.kubernetes.io/name: opet-postgres
    app.kubernetes.io/component: database
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: opet-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
